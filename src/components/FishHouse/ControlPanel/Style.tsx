import { styled } from '@material-ui/core';

const Root = styled('div')({
    backgroundColor: 'white',
    borderRadius: '10px',
    padding: '12px',
    height: '50%'
});

export const Styled = {
    Root
};
